arr = list(map(int,input("enter the numbers:").split(",")))
single_arr=[]
for i in range(len(arr)):
    if arr[i] not in single_arr:
      single_arr.append(arr[i])
print("single time appear element:",single_arr)
