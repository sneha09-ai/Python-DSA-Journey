arr1=list(map(int,input("enter the elemnts:").split(",")))
arr2=list(map(int,input("enter the elemnts:").split(",")))
arr=[]
for i in range(len(arr1)):
 for j in range(len(arr2)):
        if arr1[i]==arr2[j]:
            if arr2[j] not in arr:
             arr.append(arr2[j])
print("Intersection of Two Arrays:",arr)            
